
#include <vector>
#include "entity.h"

#ifndef _H_Spot
#define _H_Spot

/*
class Game;

class Spot {
public:
	Spot(Game* _game);
	Entity* get_top();
	void set_top(Entity* e);
	
private:
	Game* game;
	Entity* top;
	std::vector<Entity*> rest;
	
	int x,y;  // not used so far
};
*/

#endif
